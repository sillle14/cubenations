import { Ctx } from 'boardgame.io/src/types'
import { Coord } from './models/board'
import Space from './models/space'

function setup(ctx: Ctx, setupData: any) {
    let board: Array<Array<any>> = [];
    for (let x = 0; x < 16; x++) {
        board[x] = [];
        for (let y = 0; y < 11; y++) {

        }
    }
}

